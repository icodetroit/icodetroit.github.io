/* global describe, it */

var assert = require( 'chai' ).assert;

// var me = require( '..' );

describe( 'It', function() {

	it( 'is an awesome module', function() {
		assert( true, true );
	} );

} );
